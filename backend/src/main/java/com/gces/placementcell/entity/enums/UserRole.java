package com.gces.placementcell.entity.enums;

/**
 * User roles in the Placement Management System.
 * Corresponds to enum user_role in DB schema.
 */
public enum UserRole {
    STUDENT,
    PLACEMENT_OFFICER,
    ADMIN,
    RECRUITER
}
