package com.gces.placementcell.entity.enums;

/**
 * Account status values in the Placement Management System.
 * Corresponds to enum account_status in DB schema.
 */
public enum AccountStatus {
    PENDING,
    ACTIVE,
    INACTIVE,
    SUSPENDED
}
